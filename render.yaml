services:
  - type: web
    name: fastapi-resume-analyzer
    runtime: python
    pythonVersion: 3.12.6
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
